/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['@estacion33/ui', '@estacion33/core', '@estacion33/tokens'],
};

export default nextConfig;
